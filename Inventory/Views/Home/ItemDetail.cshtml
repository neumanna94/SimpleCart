<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Simple Template</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    <script src="~/js/jquery-3.2.1.min.js"></script>
    <link rel='stylesheet' href="~/css/styles.css">
  </head>
  <body>
      <div class = "container">
          <div class = "row">
            <div class = "col-md-12">
            <h2> Results </h2>
            @if (Model != null)
            {
                  <h4>Name:@Model.GetName()</h4>
                  <h4>Cost:@Model.GetCost()</h4>
                  <h4>PostDate:@Model.GetPostDate()</h4>
                  <button type="button" class="btn"><a href="/">Home</a></button>
                  <button type="button" class="btn"><a href="/delete/@Model.GetId()">Delete this Inventory Item</a></button>
            }
            </div>
        </div>
      </div>
      <div id="footer"><br> Alexander Neumann & Tim Firman Epicodus February 19th, 2018</div>
  </body>
